declare type coords = {
    x: number;
    y: number;
};
export default function makePattern(svg: string, width: number, height: number, patternWidth?: number, patternOffset?: coords, backgroundColor?: string): string;
export {};
